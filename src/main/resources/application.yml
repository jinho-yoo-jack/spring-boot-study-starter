spring:
  application:
    name: my-spring-study-diary

# 로깅 설정
logging:
  level:
    root: INFO
    com.study.myspringstudydiary: DEBUG

# 서버 설정
server:
  port: 7777
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true